[physics]
initial_condition = "low"
N = 512
param_values = [0.1]
initial_energy = 0.45
system_type = "springs"
nonlinear = "alpha"
delta_values = [0.103]
boundary = "periodic"

[output]
base_dir = "results/raw"

[experiment]
name = "test_quick"
description = "Minimal smoke test — delete after verification"

[simulation]
downsample = 1
save_every = 1
T_block = 5.0
DT = 0.05
TMAX = 10.0
debug = false
